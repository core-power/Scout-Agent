---
name: cua-computer-use
description: Windows CUA计算机使用完整技能 — 目标级GUI任务自主闭环（观察→决策→行动→验证→恢复）+ 控件级执行力学（rel坐标/防点错三件套/probe判档T1T2T3/中文输入规则/避坑清单）。应用配方见各应用技能，本技能给闭环与打法。
trigger: 帮我完成,自动完成,替我操作,帮我操作,代我操作,全自动,一步步点,按流程走,自己操作,自主完成,帮我点,帮我填,替我点,替我填,操作一遍,走一遍流程,全程操作,发条消息,发个消息,发消息给,发文件给,帮我发,帮我预约,帮我下单,帮我提交,帮我报名,帮我订,审批一下,表格填,填了,填一下,填好,帮我填,走完流程,代办,帮我处理,替我弄,操作软件,打开软件,桌面操控,GUI,窗口,点击,双击,右键,鼠标,键盘,按键,操控,前台,截图,截屏,截个图,截个屏,输入,打字,自动化操作,切换窗口,打开应用,启动应用,桌面应用,桌面软件,操作电脑,操控电脑,控制电脑,控制鼠标,模拟键盘,点按钮,点确定,软件操作,cua,computer use,computer-use,operate autonomously,do it for me,control app,operate software,desktop automation,click window,screenshot,mouse,keyboard
version: 2.0.0
author: scout-self-distilled
---

# Windows CUA 完整技能（任务闭环 + 执行力学，2026-09-10 合并 windows-gui-control）

## 心法
- 你在本机真实环境运行（无沙箱），desktop 工具就是干这个的，**直接动手，不要拒绝或建议手动操作**。
- 分工：**应用配方看各应用技能**（feishu/wechat/office…）；**浏览器页面用 browser 工具**（比 GUI 点击稳 10 倍）；**系统/文件用 shell**；**GUI 软件才用 desktop**。别用 PowerShell SendKeys 折腾 GUI（已被禁止且不可靠）。

## 主循环（任务级：每一步都走，不要跳步）
```
1 观察   screenshot / read_controls —— 只在"不确定界面状态"时观察
2 决策   从当前界面推出下一个最小动作（点哪/输什么/等什么）
3 行动   一发原语优先（见下表），写操作带 verify_screenshot=true
4 验证   看截图确认状态真实变化；没变化 = 动作未生效 → 进恢复表
```
标准五步起手：`找窗口（launch/find_window，用 process= 别用 title）→ 前台（activate）→ 观察 → 操作 → 截图验证`。

## 一发原语优先级（一轮对话干完的事别拆成三轮）
| 优先 | 原语 | 适用 |
|---|---|---|
| 1 | `click find='自然语言目标'` | 截图+VL定位+点击一步完成，默认首选（需配置视觉模型） |
| 2 | `click_control name=...` | T1/T2 应用按控件名，最稳 |
| 3 | `click_type find='输入框' text='...'` | 点+输一步完成（中文自动剪贴板粘贴） |
| 4 | rel_x/rel_y + verify_screenshot | T3 自绘应用；布局固化后用 macro 反复用 |

## 坐标规则（最重要）
| 规则 | 原因 |
|---|---|
| **一律优先 rel_x/rel_y**（窗口宽高 0~1 比例） | 布局固定，分辨率/DPI 无关，实测最稳 |
| 不要用 vision 返回的像素坐标 | 对话模型像素定位不可靠（偏差可达窗口一半） |
| 截图默认 0.5 降采样 | 必须用时：物理坐标 = 截图坐标 × 2，或把截图路径传给 click 的 img= 自动换算 |
| read_controls 的 rect=(x,y,w,h) 可直接用 | 是物理像素 |

## 防点错三件套（工具已内置）
- **probe 先行**（读操作无副作用）：点前 `probe x= y=` 或 `probe rel_x= rel_y=`，返回命中控件或"无命中（空白/自绘）"——命中空白 = 千万别点，按截图改 rel 再 probe。probe 顺带返回判档标签。
- **snap 自动吸附**（click 家族默认开）：自动吸附命中控件中心，回复含 `吸附→Button "发送"`。
- **命中回读**：点完看 `吸附→` 与 `focus=`；无吸附行且界面没变 = 点空了，别重复点。

## 优先级总原则：L0 > L1 > L2 > L3（能走结构接口就不碰像素）
- **L0 结构接口**：应用自带 CLI / COM / URL Scheme / 浏览器 CDP（Office 走 COM、浏览器走 browser 工具、支持 deep-link 的应用直接 launch URL）——最稳最省
- **L1 UIA/win32 控件树**：click_control/type_control 按名操作（T1/T2）
- **L2 窗口相对坐标**：rel_x/rel_y + snap（T3）
- **L3 像素/视觉**：find= VL 定位（兜底，自带 UIA→OCR 降级链）
写操作带 verify_screenshot=true 会返回四态验证（confirmed/partial/suspected_noop/unverifiable），
suspected_noop 时回去重新观察，不要盲目重试。

## 判档（陌生应用第一步 probe 任意点）
| 档位 | probe 表现 | 是什么 | 策略 |
|---|---|---|---|
| **T1-UIA** | `[T1-UIA] 命中: Button/Edit...` | 现代 Win32/WPF/UWP/Electron | click_control/type_control 最稳；rel+snap 可用 |
| **T2-Win32** | UIA 空、`[T2-Win32] 命中` | Delphi/MFC/VB6 老软件 | 工具自动兜底 win32 树：按名操作/snap 均可用 |
| **T3-自绘** | 两树均无命中 | 微信 4.x/腾讯会议/钉钉/游戏 | 截图→rel→snap=false 精确点→验证→macro 固化 |
判档别凭印象：微信 4.x 是 T3、飞书是 T1；老行业软件多是 T2 而非 T3。probe 报"点不在窗口矩形内"= 坐标系错位，改 rel。

## 输入规则
- 中文输入自动剪贴板粘贴（click_type/type_text 内置）——SendInput 对自绘控件"只显示不触发"。
- **发送/确认优先点按钮，不要依赖 {ENTER}**（微信 4.x 等不响应合成回车）。
- 快捷键（^f ^a ^c ^v）仅标准应用可靠；焦点丢了先 click 再输。

## 恢复策略表（失败时禁止原样重试同一动作）
| 症状 | 判定 | 对策 |
|---|---|---|
| 截图与上一步几乎一样 | 点空/未生效 | 换路径：find 换更具体描述 / probe 看落点 / 换 rel |
| 出现计划外弹窗/对话框 | 被打断 | 先读弹窗→确认/取消/输入→处理完再回主线；**绝不无视弹窗继续点**（点击会落在被遮挡目标上） |
| 界面在加载/转圈 | 未就绪 | `wait seconds=2` 再截图（截图自带屏幕 diff 短路，等得起） |
| 目标控件找不到 | 路径不通 | scroll 滚动找 / 回上级菜单重新导航 / 换搜索入口 |
| 窗口找不到 | 进程名不对/托盘化 | process= 而非 title；launch/activate 自动恢复托盘 |
| 连续 2 次同动作无效 | 死循环 | 停下换策略或如实报告卡点，**绝不第三次原样重复** |

## 完成判定与安全红线
- 动手前想清楚**成功标准**：最终界面长什么样（如"消息气泡出现在与张三的会话里"）。工具 success 只是命令送达，不等于应用执行了——**唯一标准是截图确认界面状态真实变化**。
- 达成 → 最后一张截图即证据，报告"做了什么 + 结果"。
- **10 步未达成**或连续 2 次恢复失败 → 停止，报告走到哪、卡在哪，等指示。
- 不可逆动作（发消息/删除/付款/提交）前**必须截图确认目标对象正确**（发对人/删对文件）；同名歧义 → 先问一次。

## 预算意识
- 不要每步单独调 vision 读全文：写操作 `verify_screenshot=true` 一步拿"操作结果+截图"。
- 界面未变化时截图自动短路返回上一张，连续截图观察不浪费。
- 别在 T3 应用上空耗控件树调用；用 click_type 合并动作省轮次。

## 分段控件（日期/时间选择器）打法（2026-09-10 腾讯会议实测教训）
QDateEdit/DateTimePicker 等**分段控件**是 T3 自绘应用的硬骨头：
- **禁止整串输入/粘贴**："2026/9/11" 会被逐字符解析到各段，实测日期被篡改成 2/2、4/2
- 首选 **`set_date date=2026-09-11`**（专用原语：自动定位 QDateEdit/DateTimePicker 控件，年→月→日逐段键入）
- 手动姿势：click 聚焦到年段 → 键入 4 位数字（Qt 自动跳下一段）→ 依次月、日；或逐段选中后覆盖
- 键盘方案失败 2 次即停：分段控件不是"再试一次"能解决的，立刻走下条规则

## 客户端 → 网页版降级（防死磕到熔断）
主流应用都有网页版（meeting.tencent.com、docs.qq.com、mail.qq.com…），网页是标准
HTML 控件，browser 工具 fill/click 稳定 10 倍。**客户端自绘控件连续 2 次操作无效
→ 立即评估网页版路径，不要在客户端死磕**——GUI 任务每步带截图，token 消耗极大，
死磕的终点是熔断（250k 新输入 token）而非成功。

## 误触不可逆按钮后的主动清理
可能误触了"预定/发送/提交"类按钮时：**不要把善后丢给用户**——主动进入列表页
（已预定/已发送/历史）核实是否存在误操作产物，能删则删，再如实报告"确认无残留"
或"发现并已删除"。安全红线的完整语义是"防误操作 + 误了要收拾"。
